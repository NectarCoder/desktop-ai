# Desktop AI

One place to access all your AI applications.
 
## Build

Install dependencies:
`npm install`

Build & run:
```bash
npm run build
npm start # You can just run npm start, auto builds
```

Development Server (hot reload supported!):
`npm run dev`